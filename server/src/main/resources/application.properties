# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/cookie_shop?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Инициализация базы данных
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Сервер
server.port=8081

logging.level.org.springframework.web.servlet.mvc=DEBUG
# Логирование для диагностики
logging.level.com.cookieshop=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG
management.endpoints.web.exposure.include=*

# CORS
cors.allowed-origins=http://localhost:3000

spring.jackson.serialization.indent-output=true
spring.jackson.serialization.indent-output=true
spring.mvc.format.print=true

# File upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB