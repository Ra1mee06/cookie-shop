# Cookie Shop 🍪

Многослойное веб-приложение для интернет-магазина печенья с Vue.js фронтендом и Spring Boot бэкендом.

## 📁 Структура проекта

```
cookie-shop/
├── client-vue/       # Vue.js фронтенд приложение
├── server/           # Spring Boot REST API сервер
├── database/         # SQL скрипты для базы данных
├── client-swing/     # Java Swing клиент (опционально)
└── docs/            # Документация проекта
```

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** (v16+)
- **Java** (JDK 11+)
- **Maven** (3.6+)
- **MySQL** (8.0+)

### Запуск базы данных

1. Создайте базу данных MySQL:
```sql
mysql -u root -p < database/schema.sql
```

Или выполните скрипт вручную:
```bash
cd database
mysql -u root -p < schema.sql
```

### Запуск бэкенда (Spring Boot)

```bash
cd server
mvn spring-boot:run
```

Сервер запустится на `http://localhost:8080`

### Запуск фронтенда (Vue.js)

```bash
cd client-vue
npm install
npm run dev
```

Фронтенд будет доступен на `http://localhost:5173`

## 🛠 Технологический стек

### Frontend
- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vue Router** - маршрутизация
- **TailwindCSS** - утилитарный CSS фреймворк
- **Axios** - HTTP клиент
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик

### Backend
- **Spring Boot 2.7.0** - Java фреймворк
- **Spring Data JPA** - работа с БД
- **Spring Security** - безопасность
- **MySQL** - реляционная база данных
- **JWT** - аутентификация

## 📦 Основные функции

- 🛒 Корзина покупок
- ❤️ Избранные товары
- 🔍 Поиск по товарам
- 👤 Профиль пользователя
- 🔐 Аутентификация и авторизация
- 📱 Адаптивный дизайн

## 🗂 Подробнее о компонентах

### Client Vue (`client-vue/`)
Vue.js SPA приложение с компонентной архитектурой. Использует Composition API и Vue Router для навигации.

### Server (`server/`)
REST API сервер на Spring Boot с JWT аутентификацией. Предоставляет эндпоинты для продуктов, пользователей и корзины.

### Database (`database/`)
SQL скрипты для создания схемы базы данных и начальных данных.

## 📝 Разработка

### Форматирование кода

**Frontend:**
```bash
cd client-vue
npm run format
npm run lint
```

**Backend:**
Используйте стандартные настройки IDE для Java (или настройте Maven форматирование).

## 🔧 Конфигурация

### Backend
Настройки находятся в `server/src/main/resources/application.properties`

### Frontend
Настройки Vite в `client-vue/vite.config.js`
Настройки Tailwind в `client-vue/tailwind.config.js`

## 📄 Лицензия

MIT License

